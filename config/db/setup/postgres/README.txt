Please run the scripts in the following order to recreate the EMF schema:

-- The contents of the temp"n" scripts will be merged into the
the relevant script once finalized by Marc and Alison

temp1. emf_keywords.setup
temp2. datasets_keywords.setup
temp3. datasets_keywords.setup

1. security.setup
2. countries.setup
3. sectors.setup
4. sources.setup
5. dataset_types.setup
6. datasets.setup

